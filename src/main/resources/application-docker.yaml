spring:
  datasource:
    url: jdbc:postgresql://user-postgres:5432/${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
  data:
    redis:
      host: redis
      port: 6379
  docker:
    compose:
      enabled: false